namespace _02_Attributes
{
    /*
    Attributes are metadata applied to code elements to describe or influence their behavior.
    They are implemented as classes inheriting from System.Attribute, stored in assemblies,
    and accessed at runtime using reflection.
    They do not execute by themselves but are used by compilers or frameworks to change behavior based on the information they carry.
     */
    internal class Program
    {
        static void Main(string[] args)
        {
        }
    }
}
